server:
  port: 8086

spring:
  application:
    name: argonaut-vector

argonaut:
  embedding:
    provider: onnx
  vector:
    provider: in_memory
    qdrant:
      host: localhost
      grpc-port: 6334
    integrallis:
      storage-path: ""
