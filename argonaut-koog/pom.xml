<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.jsanca.argonaut</groupId>
        <artifactId>argonaut-parent</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>argonaut-koog</artifactId>
    <name>Argonaut Koog</name>
    <description>Koog implementation of the Argonaut agentic mission (port 8085)</description>

    <properties>
        <kotlin.version>2.3.21</kotlin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>4.1.0</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Argonaut shared contracts -->
        <dependency>
            <groupId>dev.jsanca.argonaut</groupId>
            <artifactId>argonaut-core</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!--
            Koog core: agents, tools, event handler, strategies, ToolRegistry, AIAgent, etc.
            Version 1.1.1 (stable Maven Central). Uses Kotlin 2.3.10 and kotlinx-coroutines 1.10.2.
            Transitively includes: http-client-ktor-jvm (KtorKoogHttpClient.Factory via ServiceLoader),
            prompt-executor-model-jvm (PromptExecutor, MultiLLMPromptExecutor),
            prompt-model-jvm (Message.Assistant, MessagePart, ResponseMetaInfo), prompt-llm-jvm (LLModel).
        -->
        <dependency>
            <groupId>ai.koog</groupId>
            <artifactId>koog-agents-jvm</artifactId>
            <version>1.1.1</version>
        </dependency>

        <!--
            OpenRouter LLM client — not bundled inside koog-agents-jvm.
            Required to construct OpenRouterLLMClient for production use.
        -->
        <dependency>
            <groupId>ai.koog</groupId>
            <artifactId>prompt-executor-openrouter-client-jvm</artifactId>
            <version>1.1.1</version>
        </dependency>

        <!-- Kotlin stdlib (transitively included but declared explicitly for version pinning) -->
        <dependency>
            <groupId>org.jetbrains.kotlin</groupId>
            <artifactId>kotlin-stdlib</artifactId>
            <version>${kotlin.version}</version>
        </dependency>

        <!-- Spring Boot web layer for REST controllers and HTTP contract -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <sourceDirectory>${project.basedir}/src/main/kotlin</sourceDirectory>
        <testSourceDirectory>${project.basedir}/src/test/kotlin</testSourceDirectory>

        <plugins>
            <!--
                Kotlin Maven plugin — compiles Kotlin sources.
                allopen/spring makes @Service, @Configuration, etc. open so Spring can proxy them.
                -java-parameters: exposes Kotlin function parameter names via reflection,
                required for Koog @Tool argument binding (same role as -parameters in Java).
            -->
            <plugin>
                <groupId>org.jetbrains.kotlin</groupId>
                <artifactId>kotlin-maven-plugin</artifactId>
                <version>${kotlin.version}</version>
                <executions>
                    <execution>
                        <id>compile</id>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>test-compile</id>
                        <goals>
                            <goal>test-compile</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <jvmTarget>21</jvmTarget>
                    <args>
                        <arg>-Xjsr305=strict</arg>
                        <arg>-java-parameters</arg>
                    </args>
                    <compilerPlugins>
                        <plugin>spring</plugin>
                    </compilerPlugins>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.jetbrains.kotlin</groupId>
                        <artifactId>kotlin-maven-allopen</artifactId>
                        <version>${kotlin.version}</version>
                    </dependency>
                </dependencies>
            </plugin>

            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>4.1.0</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
