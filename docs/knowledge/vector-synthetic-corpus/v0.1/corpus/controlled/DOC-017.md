# DOC-017 — Authorization versus authentication in APIs

Authentication establishes who or what sent a request. Authorization decides whether that principal may perform the requested action on a particular resource. A valid access token is not proof that the caller may read every tenant's vector collection.

Apply authorization before returning search results or document content. Tenant identifiers supplied by a client are inputs to validate, not authority to trust. Audit denied requests without logging credentials or sensitive document content.
